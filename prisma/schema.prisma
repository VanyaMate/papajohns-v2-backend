generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_DB_URL")
}

model FoodMark {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  text  String
  color String
}
